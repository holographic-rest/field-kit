# Baseline Snapshot: 2025-12-23
**Purpose:** Reproducible baseline of prototype state BEFORE Sprint S01 changes

---

## Repository State

### Git Commit
- **Commit Hash:** `fa4462596ba4afd9bc611c4321e5bcb9530f0140`
- **Branch:** `main`

### Environment
- **Python Version:** 3.9.6
- **OS:** Darwin 24.6.0 (macOS ARM64)
- **Kernel:** Darwin Kernel Version 24.6.0: Mon Jul 14 11:30:40 PDT 2025
- **Architecture:** arm64

### Dependencies
- No `requirements.txt`, `poetry.lock`, or `Pipfile` found in repo root
- Dependencies inferred from imports:
  - Standard library: `sys`, `os`, `argparse`, `glob`, `shutil`, `pathlib`, `datetime`, `json`, `collections`
  - Custom modules: `fieldkit.store_jsonl`, `fieldkit.schemas`, `cli.FieldKitCLI`

---

## Golden Flow Execution

### Command
```bash
python3 prototype/scripts/run_golden_flow.py --fresh --data-dir prototype/data_baseline_test
```

### Results (5 runs)

| Run | Status | Runtime (wall clock) | Notes |
|-----|--------|---------------------|-------|
| 1 | ✅ PASS | ~0.05s | All assertions passed |
| 2 | ✅ PASS | ~0.051s | All assertions passed |
| 3 | ✅ PASS | ~0.050s | All assertions passed |
| 4 | ✅ PASS | ~0.050s | All assertions passed |
| 5 | ✅ PASS | ~0.050s | All assertions passed |

**Summary:**
- **Pass Rate:** 5/5 (100%)
- **Average Runtime:** ~0.050s (wall clock time)
- **Consistency:** All runs passed with identical event counts (33 events per run)
- **No Errors:** No stack traces or failures observed

### Assertions Verified
All runs verified:
- ✅ Items >= 5 (types: D, H, M, Q, Q)
- ✅ Bonds >= 2 (both executed)
- ✅ Event ordering: `bond.run_requested` < `bond.executed`
- ✅ Event ordering: `holologue.run_requested` < `holologue.completed`
- ✅ Event ordering: `holologue.completed` < `bond.proposals.presented`
- ✅ Final credits balance == 73

---

## Storage Summary

### Data Directory
- **Location:** `prototype/data_baseline_test/` (or `prototype/data/` by default)
- **Storage Type:** JSONL files (append-only for events, latest-wins for snapshots)

### Files Created
After a single golden flow run, the following files exist:

| File | Size (approx) | Purpose |
|------|---------------|---------|
| `items.jsonl` | 4.3 KB | Item snapshots (latest by id) |
| `bonds.jsonl` | 2.3 KB | Bond snapshots (latest by id) |
| `episodes.jsonl` | 456 B | Episode snapshots (latest by id) |
| `networks.jsonl` | 436 B | Network snapshots (latest by id) |
| `qdpi_events.jsonl` | 16 KB | Append-only, immutable event log |

**Total Storage:** ~23.5 KB per golden flow run

### Storage Behavior
- **Snapshot files** (items, bonds, episodes, networks): Latest write wins per `id`
- **Event file** (`qdpi_events.jsonl`): Append-only, never overwritten
- **Archive behavior:** `--fresh` flag archives existing JSONL files to `prototype/data_archive/<timestamp>/`

---

## Event Log Summary

### Total Event Count
- **Per golden flow run:** 33 events
- **Event ordering:** Maintained via `(episode_id, seq)` tuple

### Unique Event Types (Top 20)
From run logs, observed event types:

| Event Type | Count per Run | Direction | QDPI Stage |
|------------|---------------|-----------|------------|
| `store.commit` | 8 | system→field | Q |
| `credits.delta` | 9 | system→field | Q |
| `bond.draft_created` | 2 | user→field | D |
| `bond.executed` | 2 | system→field | M |
| `bond.run_requested` | 2 | user→field | Q |
| `bond.suggestions.presented` | 1 | system→field | Q |
| `item.created` | 2 | user→field | M |
| `app.first_run.started` | 1 | system→field | Q |
| `episode.created` | 1 | system→field | Q |
| `tutorial.started` | 1 | user→field | Q |
| `holologue.run_requested` | 1 | user→field | H |
| `holologue.completed` | 1 | system→field | H |
| `bond.proposals.presented` | 1 | system→field | Q |
| `ledger.opened` | 1 | user→field | Q |

**Total unique event types observed:** 14 (all canonical events from spec)

### Event Log Artifacts
- **Raw event log:** `docs/baselines/artifacts/2025-12-23/event_log_dump.jsonl`
- **Event analysis:** See logs in `docs/baselines/artifacts/2025-12-23/run_*.log`

---

## Hololink Suggestions

### Example Output (from run_01.log)

When requesting suggestions for an item, the system returns 4 suggestions:

```
Suggestions presented for item it_DDFC87C3CB654D23937A0673:
  1. [clarify_to_testable_claim] Pin down the meaning of "My First Field Item" using observable behaviors.
  2. [ground_in_experiment] Turn "Item" into a 6-step implementation checklist.
  3. [architecture_map] Where does "My First Field Item" fit in the overall data flow?
  4. [adversarial_test_cases] Turn "My First Field Item" into a 6-step implementation checklist.
```

### Characteristics
- **Count:** Always 4 suggestions per item
- **Format:** `[intent_type] prompt_text`
- **Intent types observed:** `clarify_to_testable_claim`, `ground_in_experiment`, `architecture_map`, `adversarial_test_cases`
- **Event-only:** Suggestions are presented via `bond.suggestions.presented` event; no Bond objects created until user confirms

### Holologue Proposals
After holologue completion, 4 proposals are presented:
```
  1. [clarify_to_testable_claim] Define "Holologue: plan (2 items)" with 3 concrete...
  2. [architecture_map] Show where "Key Themes" fits in the architecture w...
  3. [interaction_trace] Trace how "Action Items" flows through the system ...
  4. [spec_fragment_rules] Give 3 test cases that validate "Synthesis Plan" w...
```

---

## Artifacts Captured

All artifacts are stored in: `/docs/baselines/artifacts/2025-12-23/`

### Logs
- `run_01.log` - First golden flow run (full stdout/stderr)
- `run_02.log` - Second golden flow run (full stdout/stderr)
- `run_03.log` - Third golden flow run (full stdout/stderr)
- `run_04.log` - Fourth golden flow run (full stdout/stderr)
- `run_05.log` - Fifth golden flow run (full stdout/stderr)

### Data
- `event_log_dump.jsonl` - Complete event log from final run (33 events)

### Analysis Scripts
- `analyze_events.py` - Script to analyze event log (needs PYTHONPATH fix)

---

## Notes

### Known Issues
- **No dependency file:** Repo lacks `requirements.txt` or equivalent; dependencies inferred from imports
- **Event analysis script:** `analyze_events.py` needs PYTHONPATH adjustment to run standalone

### Observations
- **Stability:** Golden flow is highly stable (5/5 passes, consistent event counts)
- **Performance:** Very fast execution (~50ms wall clock time)
- **Determinism:** Event counts and types are consistent across runs
- **Storage:** JSONL-based storage is lightweight (~23.5 KB per run)

### Next Steps
After Sprint S01, compare:
- Golden flow pass rate (should remain 100%)
- Runtime (should not degrade)
- Event log structure (should remain compatible)
- Storage footprint (may increase with new features)

---

## Baseline Comparison Checklist

Use this checklist when comparing post-S01 state:

- [ ] Golden flow still passes (5/5 runs)
- [ ] Runtime has not degraded significantly
- [ ] Event log structure is compatible (same event types)
- [ ] Storage footprint is acceptable
- [ ] No new errors or warnings introduced
- [ ] Credits balance calculation still correct (73)
- [ ] Event ordering constraints still enforced
- [ ] Suggestions still work (4 per item)

---

**Baseline Date:** 2025-12-23  
**Baseline Commit:** `fa4462596ba4afd9bc611c4321e5bcb9530f0140`  
**Next Sprint:** S01 (Foundation Stabilization)

