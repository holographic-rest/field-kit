# Field-Kit v0.1 Environment Configuration
# Copy this file to .env and fill in your values

# Optional: enables real AI generation (otherwise uses structured stubs)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: custom data directory (default: prototype/data/)
# FIELDKIT_DATA_DIR=/path/to/data
